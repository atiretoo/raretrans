## R CMD check results

0 errors | 0 warnings | 1 note

* CRAN incoming feasibility NOTE:
  - New submission
  - "Possibly misspelled words": Tremblay, et, al — these are from the
    author citation in the Description field and are not misspellings.

## Resubmission

This is a resubmission. In this version I have:

* Fixed URL redirects in README (added trailing slashes, updated codecov URL).
* Replaced relative file URI for CODE_OF_CONDUCT.md with an absolute
  GitHub URL.

## Test environments

* local macOS Tahoe 26.4.1 (aarch64-apple-darwin20), R 4.5.3
* win-builder R-devel (Windows Server 2022, R 4.6.0 beta)
* R-hub: linux (ubuntu-latest), macos-arm64 (macos-latest), windows (windows-latest)

## Downstream dependencies

There are currently no downstream dependencies for this package.
