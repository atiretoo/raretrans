# Run the interactive Shiny application for Bayesian transition priors

Run the interactive Shiny application for Bayesian transition priors

## Usage

``` r
run_app()
```

## Examples

``` r
if (FALSE) { # \dontrun{
raretrans::run_app()
} # }
```
